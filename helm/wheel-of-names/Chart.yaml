apiVersion: v2
name: wheel-of-names
description: Wheel of Names - static PWA served by nginx-unprivileged
type: application
# Chart version: bump on any change to templates/values
version: 0.2.0
# Default image tag (when image.tag is not set)
appVersion: "1.0.0"
