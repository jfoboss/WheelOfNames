apiVersion: v1
kind: Service
metadata:
  name: {{ include "wheel-of-names.fullname" . }}
  labels:
    {{- include "wheel-of-names.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "wheel-of-names.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
